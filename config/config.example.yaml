# Server Configuration
server:
  host: localhost
  port: 8080

# Database Configuration
database:
  host: localhost
  user: myuser
  password: pass
  name: mydb
  port: 5432

# JWT Configuration
jwt:
  secret: your-very-secure-secret-key

cors:
  allow_origins:
    - http://127.0.0.1:5500
    - http://localhost:5500

# Admin
admin:
  username: "admin"
  password: "admin123"
  email: "admin@email.com"
